# import numpy as np
# import sys
# import matplotlib

input = [1, 2, 3]
weights = [0.12, 0.8, 0.5]
bias = 2

output = input[0]*weights[0] + input[1]*weights[1]+input[2]*weights[2] + bias

print(output)
